# NA

## Summary

Describe what this pull request changes and why.

Closes \#

## Checklist

The change was discussed in an issue or a discussion (not needed for
typos).

R code is formatted with Air, and `devtools::document()` has been run.

Tests are added or updated, and `devtools::check()` passes locally.

`NEWS.md` has an entry under the development version for user-facing
changes, grouped under the file it changes.

`README.Rmd` is rendered again if it changed.
